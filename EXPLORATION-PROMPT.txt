Create the conversion plan for the next manual section: **Exploration**.

This is the `wirge` docs repo — read `CLAUDE.md` first. We convert the original rule
book (`user-manual/`, which is sacred: never edit except typos, never published) into
published Diátaxis pages under `content/`. In the documentation's canonical order (the
Working Index in `content/_index.md`), **Exploration is section 9**, so the published page
is `content/reference/exploration.md` with **weight 100** (the reference weights step by
ten in Working-Index order: Trade 90, Exploration 100, Rebellion 110, …; 100 is free).

WRITE THE PLAN ONLY. Produce a markdown plan file in the repo root named analogously to
the existing plans (e.g. `EXPLORATION-TODO.md`; cf. `MANUFACTURING-TODO.md`). Do NOT
write the reference page or modify any content pages — the plan is implemented in a later
session.

Before writing, read:
- `CLAUDE.md` (naming/IP rules, weight convention, what is published vs. internal).
- The Diátaxis skill `.agents/skills/diataxis/SKILL.md`, especially
  `references/reference.rst` — reference pages must DESCRIBE ONLY.
- `MANUFACTURING-TODO.md` (structure/house style) and `content/reference/manufacturing.md`.
- `user-manual/exploration.md` (the source — read in full) and check `notes/` (especially
  any system/planet-generation prep).
- The existing pages this section overlaps (below) for real anchors before linking.

Model the plan on `MANUFACTURING-TODO.md`, including: a "How to use this plan" note and
a task-status table (per-task `Status: TODO/IN PROGRESS/DONE`); a Goal; a Source-material
section; an Overlap map (link, do not restate); a Terminology/code mapping (source term →
engine codes, and "nation" not "player"); Conventions (front matter `title`/`weight` only;
`relref` cross-links; fold any Markdoc `{% %}` shortcodes into prose/callouts; convert the
source's bare `/docs/user-manual/...` links into `relref`s to our pages; codes in backticks;
no rule duplication; never brand the game "Empyrean Challenge"); resolved Page-weight/
ordering (100); Link targets (existing vs. pending); small numbered tasks ending with a
wiring task (turn the bare "9 - EXPLORATION" Working-Index line in `content/_index.md` —
and its 9.x sub-items — into `relref` links/anchors; glossary; reciprocal links) and a
final build/verify task (`hugo --gc` clean; no `{%`; no "Empyrean Challenge"; all anchors
and relrefs resolve; correct sidebar order); and an Out-of-scope section.

Source content the plan must cover (verify against the source — don't trust this summary):
- **Ship movement**: interplanetary jumps are always 0.1 light-years (orbit-to-orbit within
  a system); interstellar jumps may start from any orbit and always end in the 11th orbit of
  the target system, with distances from the Star List; intra-binary moves are written as
  interstellar but treated as 0.2 light-years and may go orbit-to-orbit.
- **Probes**: run by sensors on a ship or colony; report (for any planet in the same system)
  the orbiting ships, orbiting colonies, surface colonies (count/mass/IDs), natural-resource
  deposits (type and approximate quantity), and the habitability number.
- **Surveys**: any ship/colony surveys the planet it is at; reports deposit count, location,
  type, and exact resource units; requires one transport + one professional unit; one turn.

Known overlaps / links to wire (verify each anchor exists before linking):
- `content/reference/ship-systems.md` — hyperdrives, space drives, sensors, transports, and
  **probes are already documented under #sensors**; link those rather than restating drive/
  sensor mechanics. Exploration owns the movement *rules* and the probe/survey *reports*.
- `content/reference/planets.md` — deposits/types and the habitability number a probe reports
  (`#natural-resources`, `#types`); `content/reference/habitability.md` for HN itself.
- `content/reference/mining.md` — what deposits feed into.
- `content/reference/game-setup.md` — the Star List / orbits referenced by jumps.
- `content/how-to/writing-orders.md` — jump/move, probe, and survey orders (page-level link;
  stub, anchors absent).
- `content/reference/glossary.md` — add/relink "probe", "survey", "jump", "orbit".

Dependency to flag explicitly in the plan: "ship movement during combat" is owned by the
not-yet-converted **Combat** page (Working-Index 11) — keep that out and link at page level
once it exists, or leave a `{{< callout type="warning" >}}` TODO.

Out of scope to state: drive/sensor unit stats (owned by `ship-systems.md`), the planet
generator and any engine code (the separate `pyre` repo), and editing the manual.
