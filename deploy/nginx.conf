# The docs are a static Hugo build served under the /docs path of
# ec.pbbgaming.com (matching the Hugo `baseURL` of https://ec.pbbgaming.com/docs).
#
# Deploy: the site is built on the server by /opt/wirge/deploy.sh (see
# deploy/README.md). Point this docs root at the build output by symlinking
# /var/www/wirge/docs -> /opt/wirge/wirge/public. With `root` set to
# /var/www/wirge, a request for /docs/... then resolves through the
# symlink to /opt/wirge/wirge/public/...

server {
    server_name ec.pbbgaming.com;
    root  /var/www/wirge;   # / -> /var/www/wirge/

    # Send bare /docs to /docs/ so relative asset paths resolve.
    location = /docs {
        return 301 /docs/;
    }

    # The documentation site (static Hugo output).
    location /docs/ {
        root  /var/www/wirge;   # /docs/ -> /var/www/wirge/docs/
        index index.html;            # serve index.html for directory URLs

        # Try the exact file, then the directory (index.html); otherwise 404.
        try_files $uri $uri/ =404;

        # Long-cache Hugo's fingerprinted assets; HTML stays revalidated.
        location ~* \.(?:css|js|woff2?|png|jpe?g|svg|ico|webp)$ {
            root       /var/www/wirge;
            expires    30d;
            add_header Cache-Control "public, immutable";
        }
    }

    # Serve Hugo's generated 404 page for missing docs pages.
    error_page 404 /docs/404.html;

    listen 443 ssl; # managed by Certbot
    listen [::]:443 ssl ipv6only=on; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/ec.pbbgaming.com/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/ec.pbbgaming.com/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

}


server {
    if ($host = ec.pbbgaming.com) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    listen      80;
    listen      [::]:80;
    server_name ec.pbbgaming.com;
    return 404; # managed by Certbot


}
