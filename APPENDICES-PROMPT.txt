Create the conversion plan for the final manual section: **Appendices**.

This is the `wirge` docs repo — read `CLAUDE.md` first. We convert the original rule
book (`user-manual/`, which is sacred: never edit except typos, never published) into
published Diátaxis pages under `content/`. The Appendices are the last entries in the
Working Index (`content/_index.md`), after Writing Orders (weight 180). NOTE: the appendices
are mostly lookup tables and definitions, and MUCH of Appendix A has ALREADY been decomposed
into existing reference pages (e.g. `mass.md`, `habitability.md`) — the Working Index links
A.6 MASS → `mass.md` and A.7 STORAGE → `mass.md#storage`. A central job of this plan is to
decide, item by item, whether each appendix entry (a) already lives on a topic page and
needs only a cross-link, or (b) is orphaned and needs a home — and only then whether a
standalone `content/reference/appendices.md` page is warranted at all.

WRITE THE PLAN ONLY. Produce a markdown plan file in the repo root named analogously to
the existing plans (e.g. `APPENDICES-TODO.md`; cf. `MANUFACTURING-TODO.md`). Do NOT write
any content page or modify existing content pages — the plan is implemented in a later
session.

Before writing, read:
- `CLAUDE.md` (naming/IP rules, weight convention, what is published vs. internal).
- The Diátaxis skill `.agents/skills/diataxis/SKILL.md`, especially
  `references/reference.rst` — reference pages must DESCRIBE ONLY. (The Appendix C sample
  documents may be a poor fit for reference; consider whether they belong in `how-to/` or as
  an annotated reference appendix — justify against the skill.)
- `MANUFACTURING-TODO.md` (structure/house style) and the finished pages that already host
  appendix material: `content/reference/mass.md`, `content/reference/habitability.md`,
  `content/reference/units.md`, `content/reference/manufacturing.md`,
  `content/reference/shortages.md`, `content/reference/farming.md`.
- `user-manual/appendices.md` (the source — read in full) and check `notes/`.

Model the plan on `MANUFACTURING-TODO.md`, including: a "How to use this plan" note and
a task-status table (per-task `Status: TODO/IN PROGRESS/DONE`); a Goal; a Source-material
section; an Overlap map (link, do not restate); a Terminology/code mapping (source term →
engine codes, and "nation" not "player"); Conventions (front matter `title`/`weight` only;
`relref` cross-links; fold any Markdoc `{% %}` shortcodes into prose/callouts; codes in
backticks; no rule duplication; never brand the game "Empyrean Challenge"); resolved
Page-weight/ordering (if a standalone page is kept, slot it after Writing Orders (180) and
above/among the decomposed appendix pages — `glossary.md` is 185, `mass.md` is 190 — pick a
value consistent with that and state it); Link targets (existing vs. pending); small numbered
tasks ending with a wiring task (wire the Appendix A/B/C lines in the `content/_index.md`
Working Index to their real homes; glossary; reciprocal links) and a final build/verify task
(`hugo --gc` clean; no `{%`; no "Empyrean Challenge"; all anchors and relrefs resolve;
correct sidebar order); and an Out-of-scope section.

Source content the plan must inventory (verify against the source — the central deliverable
is a per-item disposition table: already-homed / needs-a-home / host-here):
- **Appendix I (definitions)**: approximation (log-base-10 estimates in probe/sensor reports),
  fractions, groups, habitability number, hyper space, mass, storage. Most map to existing
  pages (mass, habitability) or to pending pages (approximation → Exploration's probes;
  groups → Manufacturing/Mining) — map each.
- **Appendix II — Quantities Represented by Units**: the "items per unit" table. Likely
  orphaned; decide its home (a strong candidate is `content/reference/units.md`). NOTE the
  source row "Light Structural" — per the recorded structural-units/TL decision, do not
  reintroduce "Light structural"; coordinate with how `units.md`/`ship-systems.md` handle it.
- **Appendix III — worked Examples** (farm group, factory group, missile/shortage timeline):
  these largely duplicate material now in `manufacturing.md`, `farming.md`, and `shortages.md`
  (and read as informal "explanation"). Decide: cross-link / relocate an example into an
  explanation page / drop — do NOT re-host them as reference.
- **Appendix IV — Sample Documents** (a sample orders file and a sample turn report): orphaned.
  Decide their home and Diátaxis mode (these are illustrative, not austere reference).

Known overlaps / links to wire (verify each anchor exists before linking):
- `content/reference/mass.md` (#storage), `content/reference/habitability.md` — Appendix I.
- `content/reference/units.md` — Appendix II quantities; `manufacturing.md` build costs.
- `content/reference/manufacturing.md`, `content/reference/farming.md`,
  `content/reference/shortages.md` — Appendix III examples.
- `content/reference/glossary.md` — definitions drawn from Appendix I.

Dependency to flag explicitly in the plan: some appendix items belong to sections not yet
converted (approximation → Exploration probes; the sample turn report exercises Espionage,
Combat, Trade, etc.). Decide whether each waits for its owning page or is homed now, rather
than inventing links.

Out of scope to state: re-converting tables already owned by existing pages (cross-link
only), any engine code (the separate `pyre` repo), and editing the manual.
