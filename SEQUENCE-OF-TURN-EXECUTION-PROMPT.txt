Create the conversion plan for the next manual section: **Sequence of Turn Execution**.

This is the `wirge` docs repo — read `CLAUDE.md` first. We convert the original rule
book (`user-manual/`, which is sacred: never edit except typos, never published) into
published Diátaxis pages under `content/`. In the documentation's canonical order (the
Working Index in `content/_index.md`), **Sequence of Turn Execution is section 16**, so the
published reference page is `content/reference/sequence-of-turn-execution.md` with **weight
170** (the reference weights step by ten in Working-Index order: Victory Conditions 160,
Sequence of Turn Execution 170, Writing Orders 180; 170 is free).

WRITE THE PLAN ONLY. Produce a markdown plan file in the repo root named analogously to
the existing plans (e.g. `SEQUENCE-OF-TURN-EXECUTION-TODO.md`; cf. `MANUFACTURING-TODO.md`).
Do NOT write the reference page or modify any content pages — the plan is implemented in a
later session.

Before writing, read:
- `CLAUDE.md` (naming/IP rules, weight convention, what is published vs. internal).
- The Diátaxis skill `.agents/skills/diataxis/SKILL.md`, especially
  `references/reference.rst` — reference pages must DESCRIBE ONLY.
- `MANUFACTURING-TODO.md` (structure/house style) and `content/reference/manufacturing.md`.
- `content/explanation/production-cycle.md` — the existing explanation page for the
  four-turn year / cadence. This is the key overlap: the SEQUENCE is the ordered list of
  stages WITHIN a turn; the production cycle is the rhythm ACROSS turns. The reference page
  should be an austere ordered list of the execution stages and link to `production-cycle.md`
  for the "why"; do NOT duplicate the cadence narrative.
- `user-manual/sequence-of-turn-execution.md` (the source — read in full) and check `notes/`.
- The other existing pages whose mechanics the stages name (below).

Model the plan on `MANUFACTURING-TODO.md`, including: a "How to use this plan" note and
a task-status table (per-task `Status: TODO/IN PROGRESS/DONE`); a Goal; a Source-material
section; an Overlap map (link, do not restate); a Terminology/code mapping (source term →
engine codes, and "nation" not "player"); Conventions (front matter `title`/`weight` only;
`relref` cross-links; fold any Markdoc `{% %}` shortcodes into prose/callouts; codes in
backticks; no rule duplication; never brand the game "Empyrean Challenge"); resolved
Page-weight/ordering (170); Link targets (existing vs. pending); small numbered tasks ending
with a wiring task (turn the "16 - SEQUENCE OF TURN EXECUTION" Working-Index line in
`content/_index.md` into a `relref` link; glossary; reciprocal links — and consider a link
from `production-cycle.md`) and a final build/verify task (`hugo --gc` clean; no `{%`; no
"Empyrean Challenge"; all anchors and relrefs resolve; correct sidebar order); and an
Out-of-scope section.

Source content the plan must cover (verify against the source — don't trust this summary):
the exact 21-stage ordered list in which a turn resolves — mining/farming production;
manufacturing production; combat; set up; dis-assembly; build change; mining change;
transfers; assembly; market/trade-station activity; surveys; probe/sensor reports;
espionage; ship movement; draft; pay/ration; rebellion; rebel increases; naming/control;
population increases; news-service reports. Preserve the order exactly — downstream pages
rely on it (e.g. combat before set up; production before combat).

Known overlaps / links to wire (verify each anchor exists before linking; many stages name
mechanics whose pages are DONE, others pending):
- `content/explanation/production-cycle.md` — cadence (primary overlap; see above).
- DONE pages to link per stage: `manufacturing.md`, `farming.md`, `mining.md`,
  `colonies-and-ships.md` (set up / transfer / assembly), `rebellion.md`, `population.md`,
  `food.md` (pay/ration).
- `content/how-to/writing-orders.md` — when orders are submitted vs. executed (page-level).
- `content/reference/glossary.md` — add/relink "turn", "stage/phase".

Dependency to flag explicitly in the plan: several stages name not-yet-converted sections
(Combat 11, Espionage 12, Control of Planets 13, Trade 8, Exploration 9 for surveys/probes/
movement). Link the DONE pages, and for the pending ones link at page level and/or leave
`{{< callout type="warning" >}}` TODOs rather than inventing links.

Out of scope to state: re-converting the production-cycle narrative (owned by
`production-cycle.md`), any engine code including the turn runner (the separate `pyre` repo),
and editing the manual.
