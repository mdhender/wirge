Create the conversion plan for the manual section: **Introduction**.

This is the `wirge` docs repo — read `CLAUDE.md` first. We convert the original rule
book (`user-manual/`, which is sacred: never edit except typos, never published) into
published Diátaxis pages under `content/`. **Introduction is section 1** in the Working
Index (`content/_index.md`), where the line currently reads `1 - [INTRODUCTION](#)` — a
planned entry whose link target does not exist yet.

This is a NARRATIVE section, not a lookup section — it does NOT belong in `reference/`. Two
facts shape the plan and must be addressed up front:
1. The site landing page (`content/_index.md`, above the Working Index) ALREADY paraphrases
   the manual's introduction (100-system cluster, 10 inhabited worlds, 25 nations per planet
   constituting a race, diplomacy/economics emphasis, fresh map each game). So a separate
   Introduction page risks duplicating the landing. The plan must DECIDE between: (a) point
   the Working-Index `#1` link at the landing/an anchor and add nothing new; (b) create a
   short `content/explanation/` page (understanding-oriented framing) and link it; or
   (c) leave it unpublished. Justify the choice against the Diátaxis skill.
2. The companion section, **History** (Working Index `2 - ~~HISTORY OF THE CLUSTER~~`), is
   struck through — i.e. deliberately NOT published — and already de-branded (no "Empyrean").
   Do NOT plan to publish History; coordinate so Introduction does not depend on it.

WRITE THE PLAN ONLY. Produce a markdown plan file in the repo root named analogously to
the existing plans (e.g. `INTRODUCTION-TODO.md`; cf. `MANUFACTURING-TODO.md`). Do NOT write
the content page or modify any content pages — the plan is implemented in a later session.

Before writing, read:
- `CLAUDE.md` (naming/IP rules — never brand the game "Empyrean Challenge"; in our own voice
  it is "Epimethean Challenge"; weight convention; published vs. internal).
- The Diátaxis skill `.agents/skills/diataxis/SKILL.md` — justify the mode (explanation vs.
  landing vs. not-published) and keep modes unmixed.
- `MANUFACTURING-TODO.md` (structure/house style) and, if the page lands in explanation,
  `content/explanation/production-cycle.md` for tone/front matter.
- `content/_index.md` (the landing + Working Index) to see exactly what is already said.
- `user-manual/introduction.md` (the source — read in full; note it brands the game
  "EMPYREAN CHALLENGE", which must NOT be reproduced in our voice).

Model the plan on `MANUFACTURING-TODO.md`, including: a "How to use this plan" note and
a task-status table (per-task `Status: TODO/IN PROGRESS/DONE`); a Goal; a Source-material
section; an Overlap map (link, do not restate — chiefly the landing); a Terminology mapping
("Epimethean Challenge" not "Empyrean Challenge"; "nation" not "player"); Conventions (front
matter `title`/`weight` only; `relref` cross-links; fold any `{% %}` shortcodes into prose;
never brand the game "Empyrean Challenge"); a resolved placement + (if applicable) weight
decision — if it lands in `content/explanation/`, choose a weight ordering it sensibly among
the existing explanation pages (Strategy 10, Production Cycle 20, Food Strategy 30) and state
it; Link targets; small numbered tasks ending with a wiring task (point the `1 - INTRODUCTION`
Working-Index link at the chosen target; reciprocal links) and a final build/verify task
(`hugo --gc` clean; no `{%`; no "Empyrean Challenge"; all anchors and relrefs resolve);
and an Out-of-scope section.

Source content the plan must cover (verify against the source): the game's premise and
framing, rephrased in our own voice and renamed per the IP rules — only to the extent it adds
anything beyond what the landing already states.

Out of scope to state: the History section (intentionally unpublished), any engine code (the
separate `pyre` repo), and editing the manual.
