Create the conversion plan for the next manual section: **Control of Planets**.

This is the `wirge` docs repo — read `CLAUDE.md` first. We convert the original rule
book (`user-manual/`, which is sacred: never edit except typos, never published) into
published Diátaxis pages under `content/`. In the documentation's canonical order (the
Working Index in `content/_index.md`), **Control of Planets is section 13**, so the
published page is `content/reference/control-of-planets.md` with **weight 140** (reference
weights step by ten in Working-Index order: Espionage 130, Control of Planets 140,
Communication 150, …; 140 is free).

WRITE THE PLAN ONLY. Produce a markdown plan file in the repo root named analogously to
the existing plans (e.g. `CONTROL-OF-PLANETS-TODO.md`; cf. `MANUFACTURING-TODO.md`). Do
NOT write the reference page or modify any content pages — the plan is implemented in a
later session.

Before writing, read:
- `CLAUDE.md` (naming/IP rules, weight convention, what is published vs. internal).
- The Diátaxis skill `.agents/skills/diataxis/SKILL.md`, especially
  `references/reference.rst` — reference pages must DESCRIBE ONLY.
- `MANUFACTURING-TODO.md` (structure/house style) and `content/reference/manufacturing.md`.
- `user-manual/control-of-planets.md` (the source — read in full) and check `notes/`.
- The existing pages this section overlaps (below) for real anchors before linking.

Model the plan on `MANUFACTURING-TODO.md`, including: a "How to use this plan" note and
a task-status table (per-task `Status: TODO/IN PROGRESS/DONE`); a Goal; a Source-material
section; an Overlap map (link, do not restate); a Terminology/code mapping (source term →
engine codes, and "nation" not "player"); Conventions (front matter `title`/`weight` only;
`relref` cross-links; fold any Markdoc `{% %}` shortcodes into prose/callouts; codes in
backticks; no rule duplication; never brand the game "Empyrean Challenge"); resolved
Page-weight/ordering (140); Link targets (existing vs. pending); small numbered tasks ending
with a wiring task (turn the "13 - CONTROL OF PLANETS" Working-Index line in
`content/_index.md` and its 13.x sub-items into `relref` links/anchors; glossary; reciprocal
links) and a final build/verify task (`hugo --gc` clean; no `{%`; no "Empyrean Challenge";
all anchors and relrefs resolve; correct sidebar order); and an Out-of-scope section.

Source content the plan must cover (verify against the source — don't trust this summary):
- **Taking control**: requires an orbiting or surface colony (or a trade station) at the
  planet; the player declares rulership with a control order; simultaneous control orders for
  one planet are broken by a random pick, the other order aborted.
- **Controlled planets**: to colonize a planet another player controls, either bombard/capture
  the existing colony, or request permission by diplomatic message in exchange for
  considerations (initial fee, a cut of mined resources, periodic gold, or exclusive trade at
  the ruler's station).
- **Relinquishing**: a player may relinquish control any time with an uncontrol order.

Known overlaps / links to wire (verify each anchor exists before linking):
- `content/reference/colonies-and-ships.md` — colonies/trade stations as the basis of control.
- `content/reference/victory-conditions.md` — "controls the most planets" is the win condition;
  this page is DONE — link it (it in turn depends on what controlling a planet means here).
- `content/reference/planets.md` — the planet being controlled.
- `content/how-to/writing-orders.md` — control, uncontrol, and colonizing-permission orders
  (page-level link; stub, anchors absent).
- `content/reference/glossary.md` — add/relink "control order", "uncontrol order", "ruler".

Dependency to flag explicitly in the plan: this page leans on three not-yet-converted
siblings — **Combat** (bombard/capture, Working-Index 11), **Communication** (diplomatic
message for permission, 14), and **Trade** (the ruler's trade station, 8). Link at page level
and/or leave `{{< callout type="warning" >}}` TODOs rather than inventing links.

Out of scope to state: the Combat, Communication, and Trade pages themselves (their own
plans), any engine code (the separate `pyre` repo), and editing the manual.
