Create the conversion plan for the next manual section: **Combat**.

This is the `wirge` docs repo — read `CLAUDE.md` first. We convert the original rule
book (`user-manual/`, which is sacred: never edit except typos, never published) into
published Diátaxis pages under `content/`. In the documentation's canonical order (the
Working Index in `content/_index.md`), **Combat is section 11**, so the published page is
`content/reference/combat.md` with **weight 120** (reference weights step by ten in
Working-Index order: Rebellion 110, Combat 120, Espionage 130, …; 120 is free).

WRITE THE PLAN ONLY. Produce a markdown plan file in the repo root named analogously to
the existing plans (e.g. `COMBAT-TODO.md`; cf. `MANUFACTURING-TODO.md`). Do NOT write the
reference page or modify any content pages — the plan is implemented in a later session.
Combat is by far the LONGEST manual section (see its 11.x sub-tree in the `content/_index.md`
Working Index); the plan must break it into many small numbered tasks — roughly one per
sub-mechanic (attack orders, raids/invasions, bombardment, defense, combat factor,
casualties, energy weapons, missiles, anti-missiles, ship movement, surrender, capture,
TL effects) — not one monolithic task.

Before writing, read:
- `CLAUDE.md` (naming/IP rules, weight convention, what is published vs. internal).
- The Diátaxis skill `.agents/skills/diataxis/SKILL.md`, especially
  `references/reference.rst` — reference pages must DESCRIBE ONLY.
- `MANUFACTURING-TODO.md` (structure/house style) and `content/reference/manufacturing.md`.
- `user-manual/combat.md` (the source — read in full) and check `notes/`.
- The existing pages this section overlaps (below) for real anchors before linking.

Model the plan on `MANUFACTURING-TODO.md`, including: a "How to use this plan" note and
a task-status table (per-task `Status: TODO/IN PROGRESS/DONE`); a Goal; a Source-material
section; an Overlap map (link, do not restate); a Terminology/code mapping (source term →
engine codes, and "nation" not "player"); Conventions (front matter `title`/`weight` only;
`relref` cross-links; fold any Markdoc `{% %}` shortcodes — the source has four `{% fnref %}`
footnotes — into prose/callouts; render the formulas/tables faithfully; codes in backticks;
no rule duplication; never brand the game "Empyrean Challenge"); resolved Page-weight/
ordering (120); Link targets (existing vs. pending); small numbered tasks ending with a
wiring task (turn the "11 - COMBAT" Working-Index line in `content/_index.md` and its full
11.x sub-tree into `relref` links/anchors; glossary; reciprocal links) and a final
build/verify task (`hugo --gc` clean; no `{%`; no "Empyrean Challenge"; all anchors and
relrefs resolve; correct sidebar order); and an Out-of-scope section.

Source content the plan must cover (verify against the source — don't trust this summary):
the attack types (raid, bombard, invasion, support) and percentage of commitment; military
supplies; raids/invasions with assault craft, assault weapons, transports, and military
robots; bombardment by colonies (distance-factor table) and by ships (range); defense
(vs soldiers, bombardment, while invading, vs raids, support); the combat-factor table and
the invasion/raid casualty formulas; damage from energy weapons, missiles, anti-missiles,
and energy shields (with their formulas); ship movement during combat; end-of-round;
surrender (6:1 odds; robots fight on); captured colonies and captured-population effects;
and TL loss on capture or 20%-population loss.

Known overlaps / links to wire (verify each anchor exists before linking):
- `content/reference/weapons.md` — energy weapons, missiles, anti-missiles, energy shields,
  assault weapons: this page already owns the weapon UNIT STATS (it explicitly defers combat
  *procedure* to "the combat reference material when that section is converted" — i.e. this
  page). combat.md links the stats; it must NOT duplicate them.
- `content/reference/ship-systems.md` — space drives (combat movement) and transports.
- `content/reference/units.md` / `content/reference/population.md` — soldier, military robot,
  assault craft, and unemployable codes; the "Differences from the rule book" notes.
- `content/reference/colonies-and-ships.md` — ships/colonies as combatants and capture.
- `content/reference/rebellion.md` — captured loyal population becoming rebels (this page is
  DONE; link `#`-anchors, don't restate).
- `content/how-to/writing-orders.md` — raid/bombard/invade/support orders (page-level link;
  stub, anchors absent).
- `content/reference/glossary.md` — add/relink combat terms (combat factor, commitment,
  assault craft, etc.).

Dependency to flag explicitly in the plan: tightly-coupled siblings not yet converted —
**Control of Planets** (capture → control, Working-Index 13), **Espionage** (12), and
**Technological Advancement** (the TL mechanic, section 7). Link at page level and/or leave
`{{< callout type="warning" >}}` TODOs rather than inventing links.

Out of scope to state: weapon/drive unit stats (owned by `weapons.md`/`ship-systems.md`),
any engine code including the combat resolver (the separate `pyre` repo), and editing the
manual.
